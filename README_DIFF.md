# v037_79 → v037_80 差分

## 修正内容

- 自己紹介シナリオの同一立ち絵連続指定を削除。
- 表情変更時のみ `characters.src` を指定。
- 立ち絵指定を削除した場面では `expression` / `expressionLabel` も削除。
- 不要な `bgEffect` / `effect` を削除。
- `scenario/data/intro_scenario_slim_report.json` を追加。

リポジトリ直下へ展開して上書きしてください。
