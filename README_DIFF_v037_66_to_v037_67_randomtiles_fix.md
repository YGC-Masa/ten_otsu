# v037_66 → v037_67 差分

## 修正内容

- タイトルのランダムタイル画像が出ない問題を修正。
- `scenario/random/imageset01.json` の `picpath` を `images/assets/ev/` に修正。
- `program/randomShows.js` にランダム画像パス解決関数を追加。
- `zIndex: "2.5"` を `zIndex: "2"` に修正。
- 画像読み込み失敗時にコンソールへ `[TENOTSU RANDOM IMAGE LOAD ERROR]` を出すようにしました。

このZIPをリポジトリ直下へ展開して上書きしてください。
